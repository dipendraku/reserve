import type { Metadata } from 'next';
import Page from '@/routes/book';
export const metadata: Metadata = { title: 'Find a Pro & Book Online — ReserveMe', description: 'Browse services from independent professionals, pick a time, and book instantly.' };
export default Page;
