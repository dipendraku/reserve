import type { Metadata } from 'next';
import Page from '@/routes/forgot-password';
export const metadata: Metadata = { title: 'Forgot Password — ReserveMe', robots: { index: false, follow: false } };
export default Page;
