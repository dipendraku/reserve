import type { Metadata } from 'next';
import Page from '@/routes/login';
export const metadata: Metadata = { title: 'Log in — ReserveMe', description: 'Log in to your ReserveMe business cockpit.', robots: { index: false, follow: false } };
export default Page;
