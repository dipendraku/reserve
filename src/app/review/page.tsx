import type { Metadata } from 'next';
import Page from '@/routes/review';
export const metadata: Metadata = { title: 'Leave a Review — ReserveMe', robots: { index: false, follow: false } };
export default Page;
