import type { Metadata } from 'next';
import Page from '@/routes/reset-password';
export const metadata: Metadata = { title: 'Reset Password — ReserveMe', robots: { index: false, follow: false } };
export default Page;
