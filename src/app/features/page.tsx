import type { Metadata } from 'next';
import Page from '@/routes/features';
export const metadata: Metadata = { title: 'Features — ReserveMe', description: 'Everything service providers need to publish services, manage schedules, take bookings, and grow with ReserveMe.' };
export default Page;
