import type { Metadata } from 'next';
import Page from '@/routes/signup';
export const metadata: Metadata = { title: 'Sign Up Free — ReserveMe', description: 'Create your free ReserveMe booking page in minutes.' };
export default Page;
