import type { Metadata } from 'next';
import Page from '@/routes/verify-otp';
export const metadata: Metadata = { title: 'Verify Email — ReserveMe', robots: { index: false, follow: false } };
export default Page;
